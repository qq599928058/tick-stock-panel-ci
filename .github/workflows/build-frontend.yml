name: build-frontend-dist

# 只做一件事：在 GitHub 的 runner 上把 tick-stock-panel 前端构建出来，
# 产物 (frontend/dist) 作为 artifact 上传 —— 本机内存不够跑 vite build（OOM 被内核杀）。

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.10.0
          package_json_file: frontend/package.json

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: pnpm install
        working-directory: frontend
        run: pnpm install --frozen-lockfile

      - name: pnpm build
        working-directory: frontend
        # 以 /panel/ 为 base 构建：本机只有 8000 端口对外可达（3083/3084 实测外部超时），
        # 面板要挂在 https://<host>:8000/panel/，所以静态资源与路由都要带这个前缀。
        run: pnpm exec vite build --base=/panel/

      - name: 上传 dist
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist
          retention-days: 30
